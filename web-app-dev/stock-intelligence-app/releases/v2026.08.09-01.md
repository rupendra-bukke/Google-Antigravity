# Release Note: v2026.08.09-01

## Version

- `v2026.08.09-01`

## Date

- `2026-08-09`

## Branch and Commit Info

- Source branch: `cursor/phase-a-stabilize-935c`
- Target branch: `main`

## Summary

- Phase A stabilization: documentation aligned with Supabase auth, FinNifty added to dashboard selector, dead components removed, env examples completed, and basic CI added.

## Changes

### Added
- FinNIFTY (`^CNXFINSERVICE`) to dashboard `IndexSelector`.
- GitHub Actions CI workflow: `stock-intelligence-ci.yml` (Python compile + TypeScript check).
- `GEMINI_API_KEY` and `CHECKPOINT_CRON_SECRET` in `backend/.env.example`.
- `BACKEND_URL` in `frontend/.env.local.example`.

### Changed
- `README.md` and `ARCHITECTURE.md` updated for Supabase auth layer and current routes.
- Removed unused components: `CandlestickChart`, `AdvancedDecision`, `DecisionBadge`.

### Fixed
- Documentation gaps around auth env vars and local dev bypass (`AUTH_REQUIRED=false`).

## Validation (must be checked)

- [ ] Preview tested on Vercel
- [ ] Production smoke test completed
- [ ] FinNIFTY appears in index selector and loads data
- [ ] CI workflow passes on PR
- [ ] Login still works after merge

## Rollback

- Previous stable main commit: (record at merge time)
- Rollback: `git revert <merge_commit_sha>` then `git push origin main`

## Notes

- Candlestick chart can be re-added later as an optional lazy-loaded panel (Phase B).
- `IndicatorCard.tsx` remains unused but was not removed in this phase.
